export const CORE_PRINCIPLES = [
  "append-only events",
  "deterministic replay",
  "transport != semantics",
  "renderers are pure",
  "ephemeral is explicit",
  "human + AI parity",
] as const;

export const DEFAULT_SPACES = ["canvas", "wiki", "forum", "scene"] as const;
